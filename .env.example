# Matomo analytics (public — embedded in browser bundle, not secrets)
# Production values are also mirrored in wrangler.toml [vars] so CI builds
# pick them up without needing extra GitHub secrets.
PUBLIC_MATOMO_URL=https://statistics.tangoparty.net
PUBLIC_MATOMO_SITE_ID=15
