# Tasks: Add Favorites

## 1. Test Coverage

- [x] 1.1 Add unit tests for `src/lib/utils/favorites.ts` (normalize, migrate, match).
- [x] 1.2 Add store persistence tests for `src/lib/stores/favorites.svelte.ts`.

## 2. Core Implementation

- [x] 2.1 Create `src/lib/utils/favorites.ts` and `src/lib/stores/favorites.svelte.ts`.
- [x] 2.2 Extend `Filters` and `EventStore.applyFilters()` for `favoritesOnly`.

## 3. UI

- [x] 3.1 Create `FavoriteToggle.svelte` and `EventFavoriteChooser.svelte`.
- [x] 3.2 Wire toggles into event, organizer, venue, and DJ cards/detail pages.
- [x] 3.3 Add home/kalender favorites chip and `/favoriten` route + nav item.

## 4. Validation

- [x] 4.1 Run `npm run check`.
- [x] 4.2 Run `npm run build`.
