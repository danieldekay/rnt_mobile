# Tasks

## Prerequisites

- [x] `desktop-layout-foundation` applied.
- [x] `desktop-blog` applied (provides `BlogPost` type and `posts.ts` API module).

---

## Task List

- [x] T1 — Add `fetchAnnouncements` to `posts.ts` in `src/lib/api/posts.ts`.
- [x] T2 — Create resilient route load in `src/routes/ankuendigungen/+page.ts`.
- [x] T3 — Create route UI in `src/routes/ankuendigungen/+page.svelte` with featured post, compact list, archive sidebar navigation, and mobile single-column behavior.
- [x] T4 — Verify prerender via `npm run build`.
