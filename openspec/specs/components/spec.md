# Components Specification

> Generated by spec-gen on 2026-05-10
> Source files: src/lib/components/*.svelte

## Purpose

The Components domain contains reusable Svelte components that build the user interface of the RNT Mobile application. These components encapsulate presentation logic, styling, and behavior for common UI elements like event cards, filters, maps, consent dialogs, and layout structures.

## Requirements

### Requirement: Event Card Component
The system SHALL provide a reusable component for displaying event information in a card format.

#### Scenario: Display event core information
- **GIVEN** a valid TribeEvent object
- **WHEN** the EventCard component is rendered
- **THEN** the event's title, date, time, location, and cost are displayed
- **AND** the event's type and music badges are shown if available
- **AND** the event organizer or DJ name is displayed if available

#### Scenario: Handle event image display
- **GIVEN** an event with an image URL
- **WHEN** the EventCard component is rendered with showImage=true
- **THEN** the event image is displayed in the card
- **AND** the image is sized appropriately and lazy-loaded
- **AND** hovering over the card applies a scale transformation

#### Scenario: Handle missing event data gracefully
- **GIVEN** an event with missing or null data fields
- **WHEN** the EventCard component is rendered
- **THEN** missing venue city shows "Ort offen"
- **AND** missing cost is handled appropriately
- **AND** missing organizer/DJ information is not displayed
- **AND** the component does not throw errors due to null values

#### Scenario: Apply interactive states
- **GIVEN** the EventCard component is rendered
- **WHEN** the user hovers over the card
- **THEN** the card border changes to accent color and shadow is applied
- **AND** the external link icon translates and changes color on hover
- **AND** the card maintains accessible focus states

### Requirement: Filter Components
The system SHALL provide components for filtering events by type, music, and date.

#### Scenario: FilterChip component functionality
- **GIVEN** a FilterChip component with an event type and active state
- **WHEN** the component is rendered
- **THEN** it displays the event type label with appropriate badge styling
- **AND** when active, it uses accent colors; when inactive, it uses muted colors
- **AND** clicking the chip toggles the filter state via the onclick handler

#### Scenario: MusicFilterChip component functionality
- **GIVEN** a MusicFilterChip component with a music type and active state
- **WHEN** the component is rendered
- **THEN** it displays the music type label with appropriate badge styling
- **AND** when active, it uses accent colors; when inactive, it uses muted colors
- **AND** clicking the chip toggles the music filter state

#### Scenario: DateFilter component functionality
- **GIVEN** a DateFilter component with an active date filter value
- **WHEN** the component is rendered
- **THEN** it displays radio buttons for today, week, month, and all options
- **AND** the active option is visually selected
- **AND** selecting an option calls the onchange handler with the new value

### Requirement: Map and Location Components
The system SHALL provide components for displaying events on a map and handling location consent.

#### Scenario: ConsentPlaceholder component functionality
- **GIVEN** a ConsentPlaceholder component with title, description, and action label
- **WHEN** the component is rendered
- **THEN** it displays a card with the title, description, and action button
- **AND** clicking the action button calls the onEnable handler
- **AND** the component provides clear visual indication of its purpose

#### Scenario: Calendar component functionality
- **GIVEN** a Calendar component
- **WHEN** the component is rendered
- **THEN** it displays a calendar interface for date selection
- **AND** it allows navigation between months
- **AND** it highlights dates with events
- **AND** selecting a date returns the selected date value

### Requirement: Layout Components
The system SHALL provide components for structuring the page layout.

#### Scenario: Sidebar components functionality
- **GIVEN** LeftSidebar or RightSidebar components
- **WHEN** the components are rendered
- **THEN** they provide collapsible sidebar regions for additional content
- **AND** they can be shown or hidden based on application state
- **AND** they contain appropriate navigation or informational content

#### Scenario: Layout wrapper components functionality
- **GIVEN** DesktopLayout component
- **WHEN** the component is rendered
- **THEN** it provides a layout structure suitable for larger screens
- **AND** it positions main content and sidebars appropriately
- **AND** it responds to screen size changes

### Requirement: Legal Document Components
The system SHALL provide components for displaying legal and informational documents.

#### Scenario: Legal document component hierarchy
- **GIVEN** LegalPage, LegalHeader, LegalNav, and LegalDocument components
- **WHEN** the legal document route is accessed
- **THEN** the components work together to display the document
- **AND** LegalHeader shows the document title
- **AND** LegalNav provides navigation between document sections
- **AND** LegalDocument displays the formatted document content
- **AND** LegalPage coordinates the overall layout

### Requirement: Loading State Components
The system SHALL provide components for displaying loading states.

#### Scenario: SkeletonCard component functionality
- **GIVEN** a SkeletonCard component
- **WHEN** the component is rendered
- **THEN** it displays a placeholder card with animated loading indicator
- **AND** it matches the dimensions of the actual EventCard component
- **AND** it provides visual content loading feedback
- **AND** it disappears when actual content is ready to display

## Technical Notes

- **Implementation**: `src/lib/components/` directory containing `.svelte` files
- **Component Architecture**: Each component is a self-contained Svelte file with script, markup, and style sections
- **Props Interface**: Components define strongly-typed props interfaces using TypeScript
- **Reactivity**: Uses Svelte 5 runes ($state, $derived, $effect) for reactive programming
- **Styling**: Utilizes Tailwind CSS utility classes for responsive design
- **Event Handling**: Uses Svelte's event handling directives (onclick, onchange, etc.)
- **Slots**: Components may use slots for content composition where appropriate
- **Accessibility**: Components include appropriate ARIA attributes and keyboard navigation
- **Performance**: Components are designed to be reusable and efficient
- **Image Handling**: Images use lazy loading and appropriate sizing for performance
- **Internationalization**: Uses date-fns with locale support for German date formatting
- **Theme Integration**: Uses CSS variables and Tailwind classes for theming